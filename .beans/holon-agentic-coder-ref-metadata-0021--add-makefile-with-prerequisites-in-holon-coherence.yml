id: holon-agentic-coder-ref-metadata-0021
title: "Add Makefile with prerequisite checks to holon-coherence"
type: task
status: done
assignee: antigravity
description: >
  Add a Makefile to holon-coherence with prerequisite checks (Docker CLI, buildx,
  daemon running status, and Conda) inspired by agentic-knowledge-base, along with
  core build-image, test, lint, clean, and help targets.
created_at: 2026-09-15
updated_at: 2026-09-15
notes:
  - "Prerequisites checked: Docker (CLI, buildx, daemon) and Conda (which manages python/uv). Git omitted as repo is already checked out."
  - "Check reports status with clear installation commands for missing tools, without auto-installing."
  - "Includes build-image target wrapping build_image.sh / docker-bake.hcl."
resolution: >
  Created Makefile and config/config.mk in holon-coherence worktree feat-add-makefile-prerequisites.
  Implemented check-prerequisites (verifying Docker CLI, Docker buildx, Docker daemon running status, and Conda),
  build-image, test, test-docker, lint, fix-ruff, clean, and help targets.
  Integrated taskipy in pyproject.toml ([tool.taskipy.tasks]) to drive test, lint, and clean tasks.
  Created environment.yml specifying Conda environment with uv and python.
  Added unit tests in tests/test_makefile.py and tests/test_conda_environment.py (all 16 tests passing).
  Adopted modular CI workflows from agentic-knowledge-base: make.yml (testing make help/make across ubuntu/macos),
  test-hygiene.yml (pulling out lockfile validation, taskipy lint, and taskipy clean into an isolated build),
  test-unit.yml (unit test execution across ubuntu/macos), and test-integration.yml (Docker integration tests on ubuntu).
  Squashed all changes on feat/add-makefile-prerequisites into a single commit.
